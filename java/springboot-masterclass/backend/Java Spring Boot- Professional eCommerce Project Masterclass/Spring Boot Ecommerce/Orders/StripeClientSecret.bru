meta {
  name: StripeClientSecret
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/order/stripe-client-secret
  body: json
  auth: inherit
}

body:json {
  {
    "amount": 2000,
    "currency": "usd"
  }
}

settings {
  encodeUrl: true
}
