meta {
  name: Assign Realm Role to User
  type: http
  seq: 2
}

post {
  url: {{keycloak_url}}/admin/realms/{{realm}}/users/{{user_id}}/role-mappings/realm
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
  Content-Type: application/json
}

body:json {
  [
    {
      "id": "{{role_id}}",
      "name": "{{role_name}}"
    }
  ]
}

settings {
  encodeUrl: true
}
